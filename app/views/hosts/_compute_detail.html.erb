
<% new_host = @host.nil? || new_host?(@host) %>

<%= render :partial => provider_partial(compute_resource, 'base'),
            :locals => { :f => f, :compute_resource => compute_resource, :new_host => new_host }.merge(args_for_compute_resource_partial(@host)) %>

<!--Storage-->
<%= render :partial => 'compute_resources_vms/form/volumes', :locals => { :f => f, :compute_resource => compute_resource, :new_host => new_host, :item_layout => 'removable' } %>
